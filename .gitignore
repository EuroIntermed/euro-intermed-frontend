# env files — never commit real values. NOTE: every VITE_* value is public
# (shipped to the browser), but local overrides still don't belong in git.
.env
.env.*
!.env.example

# Claude Code local settings
.claude/settings.local.json

# Node / build output
/node_modules/
/dist/
/dist-widget/

# Vite / TypeScript
*.tsbuildinfo
.vite/

# OS
.DS_Store
